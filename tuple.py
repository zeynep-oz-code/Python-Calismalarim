list =[1,2,3]

tuple = (1, 'iki',3)

print(type(list))
print(type(tuple))

print(list[1])
print(tuple[2])

list =['zeynep','rumeysa','feyza']

tuple = ('harun','emine')
#it replaces the existing list and tuple with new ones

list[1] = 'zehra'
tuple[0] = 'tarık'
#lists are mutable, so their elemets can be changed. tuples are immutable,so their elements cannot be changed.if we change a tuple we get an error.(second sentences is a zero conditional sentences.)

print(list)
print(tuple)


